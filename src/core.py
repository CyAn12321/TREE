"""Tree trunk and branch generation (Member A)."""


def generate_tree(params):
    """Generate the trunk and branch hierarchy from parameter values."""
    raise NotImplementedError(
        "Member A: implement L-System or recursive generation"
    )

